import json

my_dict = {}

# load from file first and read data with this
# json.load(file)

# write data into a file (with open file first) and write with this
# json.dump(my_dict, file, indent=4)

# update file.json open it first read with json.load(file) and using this to update value
# json.update(my_dict)
